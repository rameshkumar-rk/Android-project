package com.example.yoram;import androidx.appcompat.app.AppCompatActivity;import android.database.Cursor;import android.database.sqlite.SQLiteDatabase;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.ListView;import android.widget.TextView;import android.widget.Toast;public class queryPassport extends AppCompatActivity {    EditText passportNumber;    Button query;    String pno;    TextView resultData;    SQLiteDatabase mydatabase;    Cursor cursor, cursor2;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_query_passport);        passportNumber=(EditText)findViewById(R.id.passportNumber);        query=(Button)findViewById(R.id.query);        resultData = (TextView) findViewById(R.id.resultData);        mydatabase = openOrCreateDatabase("yoram",MODE_PRIVATE,null);        query.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view) {                pno=passportNumber.getText().toString();                cursor2 = mydatabase.query("passport_userDetails",new String[]{"passport_number","validity"},"passport_number=?",new String[]{pno},null,null,null);                cursor = mydatabase.query("passport_travelDetails",new String[]{"dt_of_journey","_from","_to"},"passport_number=?",new String[]{pno},null,null,null);                String resultDataValue = "";                //Toast.makeText(getApplicationContext()," "+cursor.getCount()+" "+cursor2.getCount(),Toast.LENGTH_LONG).show();                 if(cursor.getCount()!=0 && cursor2.getCount()!=0){                     cursor.moveToFirst();                     cursor2.moveToFirst();                    resultDataValue +="Validity = "+cursor2.getString(0)+"\n";                    resultDataValue +="Date Of Journey , From , To ";                    while(!cursor.isAfterLast()) {                        resultDataValue += cursor.getString(0) + " , " + cursor.getString(1) + " , " + cursor.getString(2) + "\n";                        cursor.moveToNext();                    }                    resultData.setText(resultDataValue);                }            }        });    }}